DATABASE_URL=postgresql+psycopg://cpf:cpf@localhost:5432/cpf
CORS_ORIGINS=http://localhost:3000
ANTHROPIC_API_KEY=

# --- Admin auth ---
ADMIN_USERNAME=useradmin
# Production: set a bcrypt hash (leave ADMIN_PASSWORD empty).
#   python -c "import bcrypt;print(bcrypt.hashpw(b'YOUR_PW',bcrypt.gensalt()).decode())"
ADMIN_PASSWORD_HASH=
# Dev convenience only — used when ADMIN_PASSWORD_HASH is empty.
ADMIN_PASSWORD=P@ssw0rd2022
# Sign/verify JWTs. MUST be a long random string in production.
#   python -c "import secrets;print(secrets.token_urlsafe(48))"
JWT_SECRET=dev-insecure-change-me-0000000000000000
JWT_EXPIRE_MINUTES=720
